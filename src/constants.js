export const GAME_STATES = Object.freeze({
  waiting: 1,
  playing: 2,
  success: 3,
  failure: 4,
  replay: 5,
});

export const KEYBOARD_DIRECTIONS = Object.freeze({
  up: 1,
  down: 2,
  left: 3,
  right: 4,
});
